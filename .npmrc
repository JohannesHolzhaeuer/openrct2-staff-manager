# Disable the implicit install-time audit: it calls npm's legacy "quick audit"
# endpoint, which is retired and prints a deprecation notice on every install.
# Vulnerability checks are run explicitly with `npm run audit`, which uses the
# supported bulk advisory endpoint.
audit=false
